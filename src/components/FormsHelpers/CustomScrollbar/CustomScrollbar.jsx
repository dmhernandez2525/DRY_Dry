import React from "react";

import "./CustomScrollbar.scss";

const CustomScrollbar = ({
  id,
  name,
  userTip,
  onClick,
  onChange,
  onBlur,
  onFocus,
  disable,
  className,
  errorMes,
  styles,
  passProps,
}) => {
  return (
    <div
      onClick={() => onClick("DryCustomScrollbar")}
      className="dry-customScrollbar"
    >
      DryCustomScrollbar
    </div>
  );
};

CustomScrollbar.defaultProps = {
  id: "",
  name: "",
  userTip: "",
  onClick: null,
  onChange: null,
  onBlur: null,
  disable: false,
  className: "",
  errorMes: "",
  styles: null,
  passProps: null,
};

export default CustomScrollbar;
