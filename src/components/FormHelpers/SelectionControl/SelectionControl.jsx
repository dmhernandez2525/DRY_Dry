import React from "react";

import "./SelectionControl.scss";

const SelectionControl = ({
  id,
  name,
  userTip,
  onClick,
  onChange,
  onBlur,
  onFocus,
  disable,
  className,
  errorMes,
  styles,
  passProps,
}) => {
  return (
    <div
      onClick={() => onClick("DrySelectionControl")}
      className="dry-selectionControl"
    >
      DrySelectionControl
    </div>
  );
};

SelectionControl.defaultProps = {
  id: "",
  name: "",
  userTip: "",
  onClick: null,
  onChange: null,
  onBlur: null,
  disable: false,
  className: "",
  errorMes: "",
  styles: null,
  passProps: null,
};

export default SelectionControl;
