import React from "react";

import "./MobileMenu.scss";

const MobileMenu = ({
  id,
  name,
  userTip,
  onClick,
  onChange,
  onBlur,
  onFocus,
  disable,
  className,
  errorMes,
  styles,
  passProps,
}) => {
  return (
    <div onClick={() => onClick("DryMobileMenu")} className="dry-mobileMenu">
      DryMobileMenu
    </div>
  );
};

MobileMenu.defaultProps = {
  id: "",
  name: "",
  userTip: "",
  onClick: null,
  onChange: null,
  onBlur: null,
  disable: false,
  className: "",
  errorMes: "",
  styles: null,
  passProps: null,
};

export default MobileMenu;
