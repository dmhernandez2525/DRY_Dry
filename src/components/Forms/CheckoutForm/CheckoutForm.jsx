import React from "react";

import "./CheckoutForm.scss";

const CheckoutForm = ({
  id,
  name,
  userTip,
  onClick,
  onChange,
  onBlur,
  onFocus,
  disable,
  className,
  errorMes,
  styles,
  passProps,
}) => {
  return (
    <div
      onClick={() => onClick("DryCheckoutForm")}
      className="dry-checkoutForm"
    >
      DryCheckoutForm
    </div>
  );
};

CheckoutForm.defaultProps = {
  id: "",
  name: "",
  userTip: "",
  onClick: null,
  onChange: null,
  onBlur: null,
  disable: false,
  className: "",
  errorMes: "",
  styles: null,
  passProps: null,
};

export default CheckoutForm;
