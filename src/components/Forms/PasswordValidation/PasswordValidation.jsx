import React from "react";

import "./PasswordValidation.scss";

const PasswordValidation = ({
  id,
  name,
  userTip,
  onClick,
  onChange,
  onBlur,
  onFocus,
  disable,
  className,
  errorMes,
  styles,
  passProps,
}) => {
  return (
    <div
      onClick={() => onClick("DryPasswordValidation")}
      className="dry-passwordValidation"
    >
      DryPasswordValidation
    </div>
  );
};

PasswordValidation.defaultProps = {
  id: "",
  name: "",
  userTip: "",
  onClick: null,
  onChange: null,
  onBlur: null,
  disable: false,
  className: "",
  errorMes: "",
  styles: null,
  passProps: null,
};

export default PasswordValidation;
