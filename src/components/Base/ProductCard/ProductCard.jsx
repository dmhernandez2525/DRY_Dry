
    import React from "react";
    
    import "./ProductCard.scss";
    
    const ProductCard = ({
      id,
      name,
      userTip,
      onClick,
      onChange,
      onBlur,
      onFocus,
      disable,
      className,
      errorMes,
      styles,
      passProps
    }) => {
    return (
        <div onClick={() => onClick("DryProductCard")}
          className="dry-productCard">
          DryProductCard

        </div>
    );
    };
    
    ProductCard.defaultProps = {
        id: "",
        name: "",
        userTip: "",
        onClick: null,
        onChange: null,
        onBlur: null,
        disable: false,
        className: "",
        errorMes: "",
        styles: null,
        passProps: null
      };
      
    export default ProductCard;
    